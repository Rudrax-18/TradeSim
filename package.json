{
  "name": "tradesim-root",
  "version": "1.0.0",
  "description": "Root package for TradeSim MERN stack platform",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefer-offline --no-audit --no-fund --no-progress && npm install --prefix server --prefer-offline --no-audit --no-fund --no-progress && npm install --prefix client --prefer-offline --no-audit --no-fund --no-progress --legacy-peer-deps",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
